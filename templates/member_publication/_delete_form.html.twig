<form method="post" action="{{ path('member_publication_delete', {'id': member_publication.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ member_publication.id) }}">
    <button class="btn">Delete</button>
</form>
