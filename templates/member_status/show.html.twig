{% extends 'base.html.twig' %}

{% block title %}MemberStatus{% endblock %}

{% block body %}
    <h1>MemberStatus</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ member_status.id }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ member_status.status }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('member_status_index') }}">back to list</a>

    <a href="{{ path('member_status_edit', {'id': member_status.id}) }}">edit</a>

    {{ include('member_status/_delete_form.html.twig') }}
{% endblock %}
