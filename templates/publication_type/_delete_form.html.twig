<form method="post" action="{{ path('publication_type_delete', {'id': publication_type.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ publication_type.id) }}">
    <button class="btn">Delete</button>
</form>
